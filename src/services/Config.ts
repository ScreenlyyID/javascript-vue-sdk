
export const BASE_URL = "https://api.screenlyyid.com"
export const API_KEY = "" //an API key created in our admin dashboard. You should avoid storing your API key in FE code, this is just an example. Ideally you would call your own back end services which would call our APIs using 
                        // and call our APIs with this key from those.
